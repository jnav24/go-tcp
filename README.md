## Simple TCP Server in Go

In one terminal

```shell
go run main.go
```

In another terminal

```shell
telnet localhost 3000
```

Type and press enter, in the second terminal, to display see the message in the first terminal
